{
  "name": "training-data-gen",
  "version": "1.0.0",
  "description": "CLI tool for generating training data using OpenAI's API compatible endpoints.",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "start": "node index.ts"
  },
  "dependencies": {
    "openai": "^6.0.1"
  },
  "devDependencies": {
    "@types/node": "^25.0.3"
  }
}
